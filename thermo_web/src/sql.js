




/* 
const Pool = require('pg').Pool
const pool = new Pool({
  user: 'postgres',
  host: 'localhost',
  database: 'Teplomer',
  password: 'pepega',
  port: 5432,
});

qier = 	'SELECT teplota_value, teplota_cas FROM public.teplota v, public.teplomer tWHERE v.teplomer_id = t.teplomer_id AND t.teplomer_name =TCZLIB110 ORDER BY v.teplota_id DESC LIMIT 1'

const getMerchants = () => {
    return new Promise(function(resolve, reject) {
      pool.query(qier, (error, results) => {
        if (error) {
          reject(error)
        }
        resolve(results.rows);
      })
    }) 
  }

module.exports = {
    getMerchants
}


 */



